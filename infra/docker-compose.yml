# Local stack — Retail Order Sync Hub
#
# Phase 1: Odoo (source of truth) + dedicated Odoo Postgres + operational
# Postgres (app_db) + OTel collector (stdout). Marketplace services
# (paris_mock, pubsub-emulator, webhook_receiver) land in Phase 2/3.

name: retail-order-sync-hub

networks:
  rosh:
    driver: bridge

volumes:
  odoo_db_data:
  app_db_data:
  odoo_filestore:
  paris_mock_data:

services:
  # PostgreSQL dedicated to Odoo (Odoo manages its own databases here).
  odoo_db:
    image: postgres:16
    environment:
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: odoo
      POSTGRES_DB: postgres
    volumes:
      - odoo_db_data:/var/lib/postgresql/data
    networks: [rosh]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U odoo -d postgres"]
      interval: 10s
      timeout: 5s
      retries: 10

  # Operational Postgres for outbox/dedupe/orders/audit (our schema via Alembic).
  app_db:
    image: postgres:16
    environment:
      POSTGRES_USER: rosh
      POSTGRES_PASSWORD: change-me
      POSTGRES_DB: rosh
    ports:
      - "5433:5432"
    volumes:
      - app_db_data:/var/lib/postgresql/data
    networks: [rosh]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rosh -d rosh"]
      interval: 10s
      timeout: 5s
      retries: 10

  # Odoo 17 Community — source of truth for catalog, customers and orders.
  odoo:
    image: odoo:17
    depends_on:
      odoo_db:
        condition: service_healthy
    environment:
      HOST: odoo_db
      USER: odoo
      PASSWORD: odoo
    # Initialize the `odoo` database with sales + product modules (no demo data)
    # so the XML-RPC seed can create products, partners and sale orders.
    # Idempotent across restarts (already-installed modules are skipped).
    command: ["odoo", "--init", "base,sale_management", "--database", "odoo", "--without-demo", "all"]
    ports:
      - "8069:8069"
    volumes:
      - odoo_filestore:/var/lib/odoo
    networks: [rosh]
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8069/web/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 60s

  # OpenTelemetry collector — receives OTLP, logs spans to stdout (Phase 1).
  # Phase 6 swaps the debug exporter for Tempo + Prometheus.
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.116.1
    command: ["--config=/etc/otel-collector-config.yml"]
    volumes:
      - ./otel-collector-config.yml:/etc/otel-collector-config.yml:ro
    ports:
      - "4317:4317"   # OTLP gRPC
      - "4318:4318"   # OTLP HTTP
    networks: [rosh]

  # Pub/Sub emulator (Phase 3) — same client as real GCP, no credentials.
  pubsub-emulator:
    image: google/cloud-sdk:emulators
    command: >
      gcloud beta emulators pubsub start
      --host-port=0.0.0.0:8085
      --project=retail-order-sync-local
    ports:
      - "8085:8085"
    networks: [rosh]
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8085 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12

  # Webhook receiver (Phase 3) — signature + dedupe + bronze + publish.
  webhook_receiver:
    build:
      context: ..
      dockerfile: infra/Dockerfile.app
    command: ["python", "-m", "src.webhook_receiver"]
    depends_on:
      app_db:
        condition: service_healthy
      pubsub-emulator:
        condition: service_healthy
    environment:
      APP_DB_HOST: app_db
      APP_DB_PORT: "5432"
      APP_DB_USER: rosh
      APP_DB_PASSWORD: change-me
      APP_DB_NAME: rosh
      PUBSUB_EMULATOR_HOST: pubsub-emulator:8085
      PUBSUB_PROJECT_ID: retail-order-sync-local
      PARIS_API_SECRET: ${PARIS_API_SECRET:-change-me}
      ML_WEBHOOK_SECRET: ${ML_WEBHOOK_SECRET:-change-me}
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
    ports:
      - "8000:8000"
    networks: [rosh]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status==200 else 1)\""]
      interval: 10s
      timeout: 5s
      retries: 12

  # Outbox worker (Phase 4) — polls Odoo, enqueues, pushes to marketplaces.
  outbox_worker:
    build:
      context: ..
      dockerfile: infra/Dockerfile.app
    command: ["python", "-m", "src.outbox_worker"]
    depends_on:
      app_db:
        condition: service_healthy
      pubsub-emulator:
        condition: service_healthy
      odoo:
        condition: service_healthy
      paris_mock:
        condition: service_healthy
    environment:
      APP_DB_HOST: app_db
      APP_DB_PORT: "5432"
      APP_DB_USER: rosh
      APP_DB_PASSWORD: change-me
      APP_DB_NAME: rosh
      ODOO_URL: http://odoo:8069
      PUBSUB_EMULATOR_HOST: pubsub-emulator:8085
      PUBSUB_PROJECT_ID: retail-order-sync-local
      PARIS_BASE_URL: http://paris_mock:9100
      PARIS_API_KEY: ${PARIS_API_KEY:-change-me}
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
    networks: [rosh]

  # paris-mock — controllable stand-in for the Paris marketplace (Phase 2).
  paris_mock:
    build:
      context: ..
      dockerfile: infra/Dockerfile.app
    command: ["python", "-m", "src.paris_mock"]
    environment:
      PARIS_API_KEY: ${PARIS_API_KEY:-change-me}
      PARIS_API_SECRET: ${PARIS_API_SECRET:-change-me}
      PARIS_MOCK_DB_PATH: /data/paris_mock.db
    ports:
      - "9100:9100"
    volumes:
      - paris_mock_data:/data
    networks: [rosh]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9100/health').status==200 else 1)\""]
      interval: 10s
      timeout: 5s
      retries: 10
