name: retail_order_sync
version: "1.0.0"
config-version: 2

profile: retail_order_sync

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
docs-paths: ["docs"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  retail_order_sync:
    silver:
      +materialized: table
      +dataset: silver
      +contract:
          enforced: true
    gold:
      +materialized: table
      +dataset: gold
      +contract:
          enforced: true
